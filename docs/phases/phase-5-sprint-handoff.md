# Phase 5 Sprint Handoff (Generated Insight Views)

Orchestrated sprint: an Opus orchestrator oversees and verifies; subagents (Sonnet/Opus per task) write the code. Launch a fresh session **on Opus** (Fable is retired):

```sh
cd /Users/aravb/Developer/keeps && claude
# then: /model → Opus
```

---

## Prompt

```
You are the Opus orchestrator for Phase 5 (Generated Insight Views) of Keeps. Working directory: /Users/aravb/Developer/keeps. You orchestrate and verify — you do NOT write the feature code yourself. Spawn a subagent per task with the Agent tool (isolation:"worktree", model per the assignments below), review every diff critically before accepting it, run the gates between waves, cherry-pick reviewed commits onto main, and run the live wave with Arav. Ground truth: docs/phases/phase-5-generated-insight-views.md (the executable plan — 10 deliverables, waves A–D, with exact files/functions/acceptance), AR-1..AR-9 in docs/phases/README.md, and git log.

THE GOAL (one sentence): a user emails Keeps a natural-language insight command ("what are my insights?", "what am I waiting on?", "what is stale?", "weekly summary", "show Acme loops") and Keeps replies privately with a short ranked summary + a signed 7-day link to a mobile-friendly memo report page that groups loops (needs you / due soon / overdue / waiting on others / stale / recently done), shows source-evidence chips, and exposes per-row actions (done/dismiss/snooze/draft-nudge) that mutate state through the SAME service layer email replies use. Ranking + inclusion are deterministic; only the human-facing summary text is model-authored (AR-8).

CURRENT STATE (verified 2026-06-13 — do not re-derive):
- Phases 0–4 are ALL DONE and LIVE at https://keeps.email (canonical; aliased from the Vercel project `keeps`). The full single-user loop runs in prod: capture → extract loops → nudge/digest → approve → act (Slack/Calendar connectors via Composio). 922 tests green (DB-gated ones skip without TEST_DATABASE_URL). Phase 4 just shipped + live-verified.
- Phase 5 builds on this and is almost entirely READ-SIDE (render views of existing loops) + one service refactor. It is disjoint from the Phase 4 connector code — do not touch src/connectors/ or the connector workflow.
- The `report.requested` event is ALREADY scaffolded in src/workflows/events.ts (a Phase 3 stub: `{ userId, kind:'insights', scope?, requestedVia, inboundEmailId?, nudgeId? }`) and the question/insights router branch already emits a stub. Phase 5 makes it real. Extend that event shape per the plan's Events section; do not invent a parallel one.

CRITICAL CORRECTIONS TO THE PLAN (it was written before Phase 4 and has stale assumptions — follow THESE, not the plan's literal paths):
1. THE INTENT ROUTER IS NOT `src/agent/intent-router.ts` (that file does not exist). The real router is `src/agent/classify-intent.ts` (deterministic `classifyEmailIntent`, returns intent + optional subtype — Phase 4 added a `subtype:'connector_command'`) + `src/workflows/functions/route-email.ts` (the dispatch; `runQuestionBranch`/`runCommandBranch`/etc.). The insight classifier extends `classify-intent.ts` (add insight detection, mirroring the connector_command pre-rule) and the dispatch lands in route-email.ts. The plan's A3 "classifyInsightCommand in src/agent/intent-router.ts" → put it in classify-intent.ts (+ its test file) and wire the dispatch in route-email.ts. NOTE the existing classify-intent.ts ALREADY routes "insights"/"status"/"what are my open loops" to the `question` branch — reconcile: the insight commands now produce `report.requested` instead of the Phase 3 question stub. Look at how route-email.ts `runQuestionBranch` + `answer-question` handler work today and replace/extend that path.
2. `pnpm db:migrate` IS BROKEN BY DESIGN (no drizzle journal). The plan says "pnpm db:generate + db:migrate" — IGNORE that. Migrations are HAND-WRITTEN sequential idempotent SQL in src/db/migrations/. Next number is 0013 (0011/0012 are Phase 4 connectors). Copy the style of 0008 (CREATE TYPE in a DO $$ ... duplicate_object guard + CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS) and 0009 (ALTER TYPE audit_action ADD VALUE IF NOT EXISTS, in its OWN non-transactional file — so the report audit-action additions go in a SEPARATE file 0014, per the Phase 4 split convention). Apply to the local dev Postgres (postgres://postgres:postgres@localhost:55433/keeps via /opt/homebrew/opt/libpq/bin/psql) TWICE to prove idempotency before the live wave. Do NOT apply to prod (the orchestrator does that in the live wave). Drizzle schema additions in src/db/schema.ts mirror the existing table style; export GeneratedReport/NewGeneratedReport types.
3. The signed-link primitive ALREADY EXISTS and the report token MUST mirror it: src/approvals/tokens.ts (mint random 32-byte base64url, store only SHA-256 hash, timingSafeEqual verify) + src/approvals/links.ts (URL builder). Plan A2 (src/reports/token.ts) should follow tokens.ts exactly. Plaintext token is returned once and appears ONLY in the link.
4. The private email reply is enqueued as a NUDGE then sent via the existing sender (mirror the digest/nudge path): look at src/workflows/functions/send-digest.ts + send-nudge.ts + the nudge repository's createReplyNudge / createNudgeRow, and the Reply-To plus-routing (buildNudgeReplyTo) so the report reply itself is commandable ("done 1, snooze 2 until Monday"). The plan's "Phase 2.5 sender interface" = these.
5. shadcn UI lives in src/components/ui/{button,badge,separator,card}.tsx (Tailwind 4). Clerk session detection is `auth()` from @clerk/nextjs/server (see app/settings/page.tsx for the exact pattern: Clerk userId → user_identities join → internal users.id). NEXT_PUBLIC_APP_URL is the base for the /r/<token> link.
6. The keystone refactor (Deliverable 6 / B1): extract `mutateLoopState(...)` from `applyLoopReplyCommand` in src/loops/service.ts so the web row-action path and the email-command path share ONE mutation funnel writing identical loop_events + identical loop.updated events (differing only by metadata.source). The parity test (D2) is THE keystone — it must assert byte-identical loop_events (except metadata.source) and identical loop.updated payloads for done/dismiss/snooze across both paths. Existing src/loops/service.test.ts must still pass unchanged.

ENGINEERING GOTCHAS — put the relevant ones in every subagent prompt VERBATIM:
1. OpenAI strict structured outputs: every generateObject Zod schema has NO .default()/.optional() — all fields required, optionality via .nullable(); no nested discriminatedUnion/oneOf. (Caused a prod outage, commit 77717a3.) The model summary (summarize.ts) and the entity classifier (the only model paths) both obey this, and BOTH have deterministic no-model fallbacks so tests need no creds (AR-8). The model NEVER adds/removes/re-orders loops — summarize.ts ignores any field beyond headline/bullets.
2. Inngest determinism: the function body re-executes per step; anything random/time-based (the `now`, the minted token) MUST be created inside a step.run and read from its memoized return. Pass only primitives across step boundaries (Date → ISO, rehydrate). generate-report registers in app/api/inngest/route.ts and auto-syncs on deploy. Idempotency key per the plan: userId + kind + scope-hash + (inboundEmailId ?? 'manual') so redelivery never double-generates.
3. AR-6: `due_soon`/`overdue` are DERIVED at query time from due_at/next_check_at, never stored. Stored statuses are lifecycle only (candidate, open, waiting_on_me, waiting_on_other, blocked, snoozed, done, dismissed). The bucketing in src/reports/query.ts is a PURE function over already-fetched rows with an INJECTED clock — test bucket boundaries at t-1s / t / t+1s (due_soon 7d, needs-you <=48h, stale N=10d, recently-done 7d).
4. Token security (mirror Phase 3): random 32-byte base64url, store ONLY the sha256 hash, timingSafeEqual compare, plaintext only in the link. Expired AND unknown tokens render the SAME friendly dead-end page with HTTP 200 — no 404 leak about whether a report ever existed, no PII logging of missing tokens.
5. Live-query, NOT snapshot (Deliverable 2): generated_reports stores `scope` + the model `summary` only — never a frozen loop list. The page resolves loops live against scope + current state on each render, so a loop marked done by reply email immediately reflects. Record the justification inline in src/reports/service.ts.
6. Report links are the ONLY entry point — no dashboard, no "my reports" index, no renew/extend, hard 7-day expiry. If the user re-asks within the idempotency window, return the SAME un-expired link (don't mint a new one). (Roadmap "dashboard by accident" risk.)
7. Source-evidence on the report page: a row whose evidence is sensitive shows a LOCK affordance and links to /sign-in?next=/r/<token> when the viewer has no Clerk session — it never reveals the quote to an anonymous link-holder. Token can MUTATE (row actions, scoped to the report's user_id) but viewing sensitive evidence requires the Clerk session.

ORCHESTRATION PLAN (the plan's waves A–D map to parallel worktree subagents; each agent commits on its branch and reports the SHA; you review every diff, then cherry-pick onto main; git worktree remove + branch -D every worktree BEFORE running gates in the main tree — vitest scans .claude/worktrees and double-counts):

Wave A — schema + deterministic primitives (all parallel, disjoint files):
  A1 [sonnet]: migration 0013 (generated_reports table + generated_report_kind enum) + 0014 (audit_action ADD VALUEs: report.requested already exists? check — add report.generated/report.viewed/report.action_applied as needed) + src/db/schema.ts additions + exported types. Apply to local 55433 twice. Do NOT touch prod.
  A2 [sonnet]: src/reports/token.ts (+test) — mirror src/approvals/tokens.ts exactly (mint/verify/timingSafeEqual).
  A3 [sonnet]: src/agent/classify-intent.ts insight detection (5 deterministic patterns from the plan, + entity fallback via generateObject {kind:'entity', entity:string} ONLY when no deterministic match and a capitalized participant-like token is present; deterministic fallback returns unknown) + its tests. Reconcile with the existing insights→question routing.
  A4 [sonnet]: src/reports/query.ts + config.ts (+tests) — assembleReport({kind,scope,now,loops,loopActivity}) → ReportSections; the deterministic bucketing + importance score; injected clock; entity matching against participants/owner_text/requester_text/source quote. PURE, no DB.
  A5 [sonnet]: src/reports/reply.ts (+test) — buildReportEmail({kind,sections,link,summary}) → {subject,textBody} matching the roadmap example + the "reply with done 1..." footer. Pure strings.

Wave B — services composing Wave A (after A; B1/B4 parallel, B2 depends on B3):
  B1 [sonnet]: the mutateLoopState refactor in src/loops/service.ts (Deliverable 6). Existing service.test.ts must pass unchanged. THE keystone refactor — review hard.
  B3 [sonnet]: src/reports/repository.ts — Drizzle I/O (insertReport, findReportByTokenHash, touchReportViewed debounced, loadLoopsForScope → loops + recent loop_events for staleness). DB-injectable constructor (mirror DrizzleConnectorAccountsRepository so DB-gated tests can target 55433).
  B2 [opus]: src/reports/service.ts — createReport / loadReportByToken (returns not_found|expired|live) / applyReportRowAction (validates token → scopes to report user_id → calls mutateLoopState). Inline live-query justification comment.
  B4 [sonnet]: src/reports/summarize.ts (+test) — generateSuggestedSummary({sections,useModel}) generateObject {headline, bullets[0..3]}, drops any other field; deterministic fallback = headline + verbatim top-3. Test asserts extra model fields are dropped.

Wave C — workflow + UI (after B; parallel):
  C1 [opus]: src/workflows/functions/generate-report.ts (Inngest, triggered by report.requested) — loadLoopsForScope → assembleReport → generateSuggestedSummary → createReport (mint token in-step) → enqueue report-email nudge (mirror send-digest) → emit report.generated. Register in app/api/inngest/route.ts. Idempotency key per spec. Mint now/token inside steps.
  C2 [sonnet]: route-email.ts command/question branch dispatch — classifyInsightCommand → emit report.requested + return; clarification reply when entity matches no tracked participant. (Adapt the real router, not the plan's process-email path literally.)
  C3 [opus]: app/r/[token]/page.tsx (server component) — load via service, auth() → canViewSensitiveEvidence, emit report.viewed (debounced 5m), friendly dead-end (200) for not_found/expired.
  C4 [sonnet]: src/reports/components/{ReportHeader,ReportSection,LoopRow,SourceEvidenceChip,RowActions}.tsx — shadcn from src/components/ui, mobile-first (actions stack under row below sm), no new primitives, sparse/utilitarian. Match the square seafoam design system (seafoam #C1F5DF / ink #14140F / paper #FAFAF8; radius 0; Bricolage Grotesque) — see app/settings/connectors/page.tsx + app/get-started-stepper.tsx for tokens.
  C5 [sonnet]: app/api/reports/[token]/actions/route.ts — POST {loopId, action, snoozeUntil?} → validate token → resolve user_id → applyReportRowAction → return updated section JSON for client re-render. "draft_nudge" enqueues a pending nudge (no outbound until approved).

Wave D — YOU (the orchestrator) + one agent: end-to-end + keystone tests, full gates, live wave with Arav:
  D2 (KEYSTONE) [you review hardest]: src/loops/parity.test.ts — email-command path vs row-action path produce identical loop_events (except metadata.source) + identical loop.updated for done/dismiss/snooze. Plus D1 insights-e2e, D3 token expiry/forgery, D4 boundary-clock query tests, D5 page render smoke. (One sonnet agent may write D1/D3/D4/D5; you own reviewing D2's parity claim adversarially.)
  Then: full gates (pnpm typecheck && pnpm test && pnpm build), apply migrations 0013/0014 to prod via psql, vercel deploy --prod --yes, verify generate-report registered by behavior, then LIVE wave with Arav: email "what are my insights?" → private reply with ranked summary + /r/<token> link → open the link → memo page renders his real loops → tap "done" on a row → loop actually transitions (same as an email reply) → re-open shows it moved to Recently done. Update docs/phases/README.md Phase 5 row to done; write the Closeout section.

YOUR VERIFICATION DUTIES (non-negotiable):
- Read every subagent diff before accepting. Adversarial focus: (a) the PARITY invariant — row action and email command MUST be byte-identical state transitions (D2); (b) the MODEL BOUNDARY — query.ts decides inclusion/order deterministically, summarize.ts only writes headline/bullets and drops everything else; (c) TOKEN safety — hash-only storage, timingSafeEqual, expired==unknown==200 dead-end, no leak; (d) LIVE-QUERY — no snapshotting, a done loop reflects immediately; (e) SENSITIVE-EVIDENCE gate — anonymous link never reveals a quote that needs login.
- Run pnpm typecheck && pnpm test between waves; full pnpm build before deploy. DB-gated tests run with TEST_DATABASE_URL=postgres://postgres:postgres@localhost:55433/keeps. New code needs new tests with deterministic fallbacks — no live model creds in the default test run.
- Commit per task, atomic, message style "Phase 5 <task>: ..." ending "Co-Authored-By: Claude <noreply@anthropic.com>". Never commit secrets.
- If a subagent's work fails review twice, fix it yourself rather than spawning a third attempt.

OPS REALITIES (from Phase 4, still true): no git remote — deploy with `vercel deploy --prod --yes` (CLI authed as abharw, project keeps). Prod DATABASE_URL via `doppler secrets get DATABASE_URL --project keeps --config prd --plain`; apply prod migrations by hand via psql at /opt/homebrew/opt/libpq/bin. Vercel env vars are SENSITIVE/write-only (Doppler/Vercel are the ops path). Inngest auto-syncs on deploy (verify by behavior). Postmark is on the free 100/mo tier — report replies add volume; flag the $15/mo upgrade if the live wave pressures it (Arav defers until limited).

Working style: act, don't ask, for reversible engineering steps. Arav does: anything on his phone, prod-deploy/migration go-aheads if you want confirmation, sending real emails, the live UAT taps. Stop at the first live-smoke failure, debug yourself (psql, vercel logs) before involving him. Start with Wave A (all five tasks in parallel worktrees).
```
