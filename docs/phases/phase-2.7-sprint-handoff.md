# Phase 2.7 Sprint Handoff (Capture Aperture + Onboarding)

Orchestrated sprint: Fable oversees and verifies; subagents (Sonnet/Opus per task) write the code. Launch with a fresh session:

```sh
cd /Users/aravb/Developer/keeps && claude
```

---

## Prompt

```
You are orchestrating Phase 2.7 (capture aperture & onboarding repair) for Keeps. Working directory: /Users/aravb/Developer/keeps. You (Fable) do NOT write the feature code yourself — you spawn subagents per task with the Agent tool, review every diff critically before accepting it, run the gates between waves, commit atomically, and run the live verification with Arav. Ground truth: docs/phases/phase-2.7-capture-aperture.md (the executable plan — deliverables, acceptance criteria, task waves), AR-9 in docs/phases/README.md (binding consent-boundary ruling), git log.

CURRENT STATE (verified 2026-06-12, do not re-derive):
- Production LIVE: https://keeps.email (canonical-to-be; domain attached and serving) + https://keeps-ivory.vercel.app (current NEXT_PUBLIC_APP_URL; stays as alias). agent@keeps.email works both directions (apex MX -> Postmark inbound; DKIM/SPF/Return-Path verified; From + REPLY_TO_BASE are agent@keeps.email).
- Full pipeline verified live: inbound -> extraction (gpt-5.1) -> nudge -> "dismiss 1" reply -> dismissed. Sentinel live: alert-on-pipeline-failure (inngest/function.failed -> email) + pipeline-canary (daily 13:00 UTC; canary user test@blackhole.postmarkapp.com seeded in prod users).
- DB: RDS keeps-prod, migrations 0000-0004 applied. pnpm db:migrate is broken BY DESIGN (no drizzle journal) — apply new migrations via psql by hand (psql lives at /opt/homebrew/opt/libpq/bin). Prod DATABASE_URL: `doppler secrets get DATABASE_URL --project keeps --config prd --plain` (Vercel env vars added via piped CLI are SENSITIVE/write-only — pull returns empty; Doppler is the ops path).
- Inngest: auto-sync on deploy WORKS (Deployment Protection scoped to previews + INNGEST_SERVE_ORIGIN=https://keeps-ivory.vercel.app). Signing key: vercel env is write-only; if you need it for CLI verification, Arav can read it from the Inngest dashboard — but you should rarely need it (auto-sync is trusted; verify registration by behavior).
- Clerk dev instance; webhook live at /api/auth/clerk/webhook (user.created/user.updated -> users + user_identities + claim flow).
- Postmark server token: .env.local has it on a COMMENTED line (active line is empty = dev recording sender; keep it that way locally).
- Stepper exists at app/get-started-stepper.tsx (warm cream/terracotta Tailwind 4 design — keep the design language). KNOWN BUG: it hardcodes captureAddress "agent@keeps.ai" (domain we never owned). Sign-up currently redirects to Clerk's prebuilt <SignUp/> page — Phase 2.7 D5 replaces that with inline useSignUp hooks (email -> code, one thing at a time, mobile-first).

ENGINEERING GOTCHAS — put these in every relevant subagent prompt verbatim:
1. Inngest step determinism: the function body re-executes per step. Anything random/time-based MUST be minted inside a step.run and read from its memoized return (the pipeline canary's first prod run failed exactly this way).
2. OpenAI strict structured outputs: generateObject Zod schemas must have NO .default()/.optional() — every field required, optionality via .nullable() (commit 77717a3 fixed a prod outage caused by this).
3. Postmark: Reply-To must be a top-level field, never inside the Headers array (error 300). Outbound sends live in their own dedicated Inngest step so a failed send retries without re-running extraction (commits ae763dd, ed4664d).
4. New Inngest functions register in app/api/inngest/route.ts and auto-sync on deploy — no manual sync step.

ORCHESTRATION PLAN (waves from the phase doc; spawn agents with the Agent tool, model per task):
Wave A — 3 subagents in PARALLEL (one message, three Agent calls):
  A1 [sonnet]: src/email/auto-reply.ts isAutoGenerated + normalize.ts header capture (Auto-Submitted, Precedence, X-Auto-Response-Suppress) + the four fixtures/tests from D1. Own-domain check derives from POSTMARK_FROM_ADDRESS/POSTMARK_REPLY_TO_BASE env, never hardcoded.
  A2 [sonnet]: migration src/db/migrations/0005_phase2_7_activation_and_follow.sql per the Data section + src/db/schema.ts updates (pending_inbound_emails.activation_sent_at, audit_action values email.activation_sent / email.activation_suppressed / email.thread_followed). Do NOT run the migration — Wave C applies it via psql.
  A3 [sonnet]: docs/recipes/capture.md per D4 + src/product/capture-address.ts (NEXT_PUBLIC_CAPTURE_ADDRESS, default agent@keeps.email).
Wave B — 4 subagents in PARALLEL after Wave A is reviewed+committed:
  B1 [sonnet]: src/email/system-send.ts sendSystemEmail (RFC 3834 Auto-Submitted: auto-replied on our own auto-replies; persists outbound_emails with null nudge_id) + tests. Check the outbound_emails schema first — nudge_id may need a nullability migration; if so, coordinate: it belongs in 0005.
  B2 [sonnet]: src/workflows/functions/send-activation-email.ts per D2 (idempotency on pendingInboundEmailId; suppression via isAutoGenerated AND per-sender 7d window via activation_sent_at; audit both outcomes) + registration + tests.
  B3 [OPUS]: thread-follow per D3 — repository findThreadOwnerForFollow(threadKey, senderEmail) + the branch in handlePostmarkInboundEmail BEFORE the unknown-sender path + spoof-guard tests (stranger with stolen References must NOT attach; sender must be a prior participant on the thread). This touches the core inbound flow — highest-risk task in the sprint.
  B4 [OPUS]: onboarding stepper per D5 — inline useSignUp (create -> prepareEmailAddressVerification email_code -> in-stepper code step with inputmode=numeric autocomplete=one-time-code, resend w/ cooldown -> attemptEmailAddressVerification -> setActive) replacing the /sign-up redirect; all error states inline (existing account -> /sign-in link); capture address from src/product/capture-address.ts; mobile-first at 390px (inputs >=16px, taps >=44px, no horizontal scroll). Keep /sign-up route as fallback; do NOT touch /sign-in. Keep the existing design tokens.
Wave C — YOU (Fable), with Arav for phone/dashboard steps:
  C0: apply migration 0005 via psql (Doppler DATABASE_URL), verify enum/column. Deploy. Verify Inngest registered the new function (send a probe, not a manual sync).
  C1: flip NEXT_PUBLIC_APP_URL to https://keeps.email, redeploy, verify Clerk sign-up from the new origin + activation links use it. Then the deferred 2.6 claim-path smoke END TO END THROUGH THE NEW STEPPER ON ARAV'S PHONE: second non-user address -> email agent@keeps.email -> activation email arrives -> tap link -> inline email+code flow -> pending claims -> loop appears -> nudge sends.
  C2: thread-follow live smoke (Arav emails a counterparty with agent CC'd; counterparty replies; reply attaches to Arav's thread in DB and processes).
  C3: loop-guard probe (POST an Auto-Submitted: auto-replied fixture -> email.activation_suppressed audit row, zero outbound).
  Close out: check off 2.7 exit criteria + the 2.6 claim-path checkbox; update roadmap if anything new is covered.

YOUR VERIFICATION DUTIES (non-negotiable):
- Read every subagent diff before accepting. Subagents produce plausible-but-wrong code; you are the reviewer of record. Adversarial focus: B3's spoof guard (try to construct an attach that shouldn't happen) and B4's auth states (wrong code, expired code, existing account, double-submit).
- Run pnpm typecheck && pnpm test between waves; full build before deploy. 87+ tests must stay green; new code needs new tests (subagent prompts must demand them).
- Commit per task or per wave, atomic, message style "Phase 2.7 <task>: ..." ending with "Co-Authored-By: Claude <noreply@anthropic.com>". Never commit secrets; never print secret values.
- If a subagent's work fails review twice, fix it yourself rather than spawning a third attempt.

Working style: act, don't ask, for reversible engineering steps. Arav does: anything on his phone, Clerk/Postmark/Inngest dashboards, and sending real emails from his accounts. Stop at the first live-smoke failure, debug yourself (vercel logs, Inngest traces via behavior, psql) before involving him.
```
