# Phase 4 Sprint Handoff (Slack & Calendar Connectors — Composio)

Orchestrated sprint: Fable oversees and verifies; subagents (Sonnet/Opus per task) write the code. Launch with a fresh session:

```sh
cd /Users/aravb/Developer/keeps && claude
```

---

## Prompt

```
You are orchestrating Phase 4 (Slack & Calendar connectors) for Keeps. Working directory: /Users/aravb/Developer/keeps. You (Fable) do NOT write the feature code yourself — you spawn subagents per task with the Agent tool (worktree isolation, model per task), review every diff critically before accepting it, run the gates between waves, commit atomically, and run the live verification with Arav. Ground truth: docs/phases/phase-4-slack-calendar-connectors.md (the executable plan — 11 deliverables, waves A-E), AR-7/AR-8/AR-9 in docs/phases/README.md, git log.

CRITICAL OVERRIDE — COMPOSIO, NOT NANGO (decided 2026-06-13):
The phase-4 plan is written entirely around Nango. We are using COMPOSIO instead, because Arav already has Composio keys and managed OAuth set up — this collapses the whole external-account long pole (no Nango workspace, no Slack app, no Google OAuth app to stand up). The ARCHITECTURE is unchanged and provider-agnostic; only the connector TRANSPORT layer, the connect/settings UI, and the webhook handler differ. Concretely:
- KEEP (provider-agnostic, do exactly as the plan says): the draft -> approval_request -> step.waitForEvent approval gate (Phase 3, shipped), the AR-7 authorize() policy gate (DONE — see below), the connector-command Zod schema (DONE), the intent-router @Slack/@Calendar branch, the connector-command parser, the email templates, the handle-connector-command Inngest workflow shape, the execute-once connector_actions invariant (SELECT FOR UPDATE), audit/events, and the E1-E5 fake-transport fixtures. The transport-interface seam (SlackTransport/CalendarTransport + createFake*Transport) is the whole point — the real transport just becomes a Composio adapter.
- SWAP (Nango -> Composio): the real Slack/Calendar transports call Composio actions instead of nango.proxy; connector_accounts columns are Composio-flavored (e.g. composio_connected_account_id, composio_entity_id, external_account_email/label/scopes) NOT nango_connection_id/nango_integration_id; the connect flow uses Composio's connect/initiate-connection instead of @nangohq/frontend Connect UI; the webhook handler verifies COMPOSIO_WEBHOOK_SECRET and parses Composio's connection-event payloads.
- BEFORE building any connector transport (Wave B onward): spawn a research agent (Opus, with WebSearch/WebFetch/context7) to confirm the CURRENT Composio TypeScript SDK surface — package name (@composio/core vs composio-core), the connected-accounts/entity model, how to execute a Slack send-message and a Calendar event-insert action, how to look up a Slack user by email, and whether Composio emits a connection-revoked webhook (if not, we poll connection status on the AR-5 sweep cadence). Composio's SDK changes; do not code its API from memory. Produce a short RESEARCH-COMPOSIO.md the connector-tool agents consume.
- Confirm with Arav early (he does this): WHICH Composio connections are already authed for his identity (Slack workspace + Google Calendar), and copy COMPOSIO_API_KEY + COMPOSIO_WEBHOOK_SECRET from the `backend` Doppler project into Keeps' Vercel env (sensitive/write-only) before the live wave. `doppler secrets get COMPOSIO_API_KEY --project backend --config prd --plain`.

CURRENT STATE (verified 2026-06-13, do not re-derive):
- Phase 3 is DONE and LIVE-VERIFIED at https://keeps.email (canonical; keeps-ivory.vercel.app is alias + INNGEST_SERVE_ORIGIN). Shipped: nudge cron sweep (*/10) + send-nudge, daily digest cron (hourly) + send-digest with AR-9 coverage line + capture prompt, approval workflow (handle-approval with step.waitForEvent + 7d timeout), sweep-approval-expiry (*/15) failsafe, signed expiring approval web pages + decide route, reply approve/reject/edit/cancel through the intent router, user-local snooze (done N / snooze N until <day> resolves to user-local 9 AM). 630 tests green (22 DB-gated skipped without TEST_DATABASE_URL). All six new Inngest functions auto-synced and firing on schedule in prod (confirmed via vercel logs). Live-verified end-to-end: web-link approve, email-reply approve (both -> approval.executed audit), nudge send to inbox. Migrations 0006-0010 applied to prod RDS.
- Phase 4 FOUNDATIONS ALREADY ON MAIN (provider-agnostic, do NOT redo):
  - A2 [done]: src/policy/actions.ts — authorize(action, context, {now?}) implements the AR-7 truth table exactly (approved+unexpired -> allowed; none/pending -> needs_approval; rejected/expired/cancelled/approved-but-expired -> denied); standingGrant reserved; requiresApproval + assertApprovalAllowed are shims delegating to authorize. Full truth-table tests in src/policy/actions.test.ts.
  - A3 [done]: src/agent/schemas.ts — connectorCommandDraftSchema + connectorActionPayloadSchema + types (ConnectorCommandDraft, ConnectorActionPayload, SlackDmPayload, CalendarEventPayload). OpenAI-strict-safe: NO .default()/.optional(), optionality via .nullable(); destination is FLAT ({ kind:'person'|'self', nameText, emailText }) not a discriminatedUnion because OpenAI strict rejects nested oneOf. Tests in src/agent/schemas.test.ts.
  - A1 was DISCARDED on purpose: the Nango-coupled connector schema is NOT on main. Your FIRST Wave-A task is the Composio-flavored connector schema + migration (next sequential number is 0011; 0012 for the audit-action ADD VALUEs per the split convention).
- TEMPORARY: app/api/admin/test-approval/route.ts is a secret-guarded probe (KEEPS_ADMIN_PROBE_SECRET in Vercel prod) used to create test_action approvals during Phase 3 live verification. Remove it at Phase 4 closeout (or consciously keep — it is inert without the secret).
- DB: RDS keeps-prod, migrations 0000-0010 applied. pnpm db:migrate is broken BY DESIGN (no journal). Migrations are hand-written sequential idempotent SQL in src/db/migrations/ (copy the style of 0008 for CREATE TYPE/tables, 0009 for ALTER TYPE ADD VALUE in its own non-transactional file). Apply via psql at /opt/homebrew/opt/libpq/bin; prod DATABASE_URL: `doppler secrets get DATABASE_URL --project keeps --config prd --plain`. A local dev Postgres at postgres://postgres:postgres@localhost:55433/keeps has 0000-0012-era schema applied (re-apply your new migrations there to verify idempotency before the live wave). Vercel env vars are SENSITIVE/write-only — Doppler is the ops path; there is NO git remote, deploy with `vercel deploy --prod --yes` (CLI authed as abharw, project keeps).
- Inngest: auto-sync on deploy WORKS. Registration = add the function to the array in app/api/inngest/route.ts, deploy, verify by behavior (probe/logs), never manual sync. Existing live functions include process-email, send-activation-email, the four Phase 3 sweeps/senders, handle-approval. Your handle-connector-command + any hydration/sweep functions register the same way.
- Outbound mail: nudge sends via sendNudge (src/loops/send-nudge.ts); non-nudge system mail via sendSystemEmail (src/email/system-send.ts, sets Auto-Submitted: auto-replied). findSendableNudge has a PRIVACY GUARD (never addresses a nudge to a non-owner address) — preserve it. Postmark is on the free 100/mo tier; digests + nudges already pressure it — Arav should upgrade to the $15/mo tier before any volume.

ENGINEERING GOTCHAS — put these in every relevant subagent prompt verbatim:
1. Inngest step determinism: the function body re-executes per step. Anything random/time-based MUST be minted inside a step.run and read from its memoized return. For crons: mint `now` once in the first step. Pass only primitives across step boundaries (Date fields JSON-round-trip to strings — return ISO and rehydrate).
2. Outbound/external sends live in their own dedicated SEND-ONLY step — no DB writes in the same step — with bookkeeping in a following step reading the memoized result. The execute-once invariant: load connector_actions row -> SELECT FOR UPDATE -> if completed/executing return cached result, else set executing, call Composio, write result+completed+executed_at, COMMIT. Same approval event delivered twice => exactly one Composio call.
3. OpenAI strict structured outputs: generateObject Zod schemas must have NO .default()/.optional() — every field required, optionality via .nullable(); no nested discriminatedUnion/oneOf (flatten). (Caused a prod outage, commit 77717a3; the connector schema already follows this.)
4. New Inngest functions register in app/api/inngest/route.ts and auto-sync on deploy. AR-5: NO step.sleepUntil anywhere in src/workflows/ — sweeps + waitForEvent only. The Calendar confirmation-window uses waitForEvent with a short timeout, not sleep.
5. Every time-dependent function takes injected `now` (mirror parseLoopReplyCommand). Timezone math via Intl.DateTimeFormat with the user's IANA tz (users.timezone exists), no new deps; unknown tz falls back to UTC.
6. AR-7 (the security boundary): NO connector action executes without authorize(action, { userId, approval }) returning 'allowed' against the LIVE approval row. Never bypass it — not even for a fixture. authorize() and the execute funnel pattern already exist (src/policy/actions.ts, src/approvals/execute.ts) — reuse them.
7. AR-9 (consent boundary): the calendar-metadata capture prompts (Deliverable 11 — post-meeting "any commitments?" + pre-meeting brief) may use ONLY calendar metadata (title/attendees/times — NEVER message content) plus captured-loop data; every prompt must pass the one-sentence provenance test. They count toward the Phase 3 daily outbound caps. Free-text replies route through the existing capture branch.
8. Approval tokens: random 32-byte base64url, store only the SHA-256 hash, plaintext only in the email link; decisions idempotent (double-decide = no-op). All shipped in Phase 3 (src/approvals/tokens.ts, service.ts) — reuse, do not reinvent.

ORCHESTRATION PLAN (adapt the phase doc's waves to Composio; tasks within a wave run in PARALLEL via isolation:"worktree", each agent commits on its branch and reports it, cherry-pick after review, git worktree remove + branch -D every worktree BEFORE running gates in the main tree — vitest scans .claude/worktrees and double-counts):
Wave 0 — research (1 Opus agent): RESEARCH-COMPOSIO.md confirming the current Composio TS SDK surface (see CRITICAL OVERRIDE). Everything in Wave B's transports depends on it.
Wave A — foundations (A2/A3 already DONE; remaining, parallel):
  A1 [sonnet]: Composio-flavored connector schema + migration 0011 (connector_accounts with composio columns + connector_actions with unique(idempotency_key)) + 0012 (audit_action ADD VALUEs: connector.account_connected/revoked/auth_error, connector.action_requested/executed/failed, connector.recipient_ambiguous, policy.authorize_denied) + schema.ts types. Apply to local 55433 twice to verify idempotency. Do NOT apply to prod.
  A4 [sonnet]: src/connectors/composio.ts — env validation (COMPOSIO_API_KEY/COMPOSIO_WEBHOOK_SECRET into src/config/env.ts), singleton Composio client, createConnectSession/getConnection/verifyComposioWebhookSignature equivalents, per Wave 0 research.
Wave B — connector tools + intent router (after Wave 0 + A; parallel):
  B1 [opus]: Slack tool behind SlackTransport interface + createFakeSlackTransport; resolveSlackUser (lookup by email, list fallback, ambiguous/not_found), executeSlackDm. Real transport = Composio Slack actions.
  B2 [sonnet]: Calendar tool behind CalendarTransport + fake; createEvent/executeCalendarEvent. Real transport = Composio Calendar actions.
  B3 [sonnet]: src/agent/parse-connector-command.ts — generateObject against connectorCommandDraftSchema (AR-8) + deterministic regex fallback (useModel:false) for fixtures.
  B4 [sonnet]: intent-router @Slack/@Calendar branch — deterministic ^\s*@(Slack|Calendar)\b short-circuit to command/connector_command, else model; emits connector.action_requested. (Router today is src/workflows/functions/route-email.ts + src/agent/classify-intent.ts — adapt; the plan's src/loops/intent-router.ts path does not exist here.)
  B5 [sonnet]: outbound email templates (connector-missing, connector-ambiguous, connector-approval reusing Phase 3 token/link helpers, connector-reconnect).
Wave C — Composio plumbing (after A4; parallel):
  C1 [sonnet]: Composio connect server actions (start Slack/Calendar connect, reconnect) — Clerk-gated, tie to user id via Composio entity.
  C2 [opus]: webhook handler app/api/connectors/composio/webhook/route.ts — verify COMPOSIO_WEBHOOK_SECRET, parse connection events, upsert connector_accounts, emit connector.connected/revoked, audit. (If Composio has no revoke webhook per Wave 0, build a status-poll sweep instead and note it.)
  C3 [sonnet]: connector-account hydration Inngest step (connector.connected -> read connection details -> fill external_account_email/label/scopes). Idempotent.
Wave D — workflow + UI (after B+C):
  D1 [opus]: handle-connector-command Inngest function (Deliverable 9 a-e) — load command+account (missing -> connect-link reply), resolve recipient (ambiguous -> clarification reply), create draft+approval (reuse Phase 3), Slack: approval email + waitForEvent('approval.received', match data.approvalId, 7d); Calendar: confirmation email + waitForEvent short timeout (default execute-on-timeout per locked roadmap default), authorize() then execute-once.
  D2 [opus]: execute-once transaction src/connectors/execute.ts (SELECT FOR UPDATE, branch on status, authorize, dispatch to Slack/Calendar tool, write result+status in one tx). Twice-concurrent test asserts one transport call.
  D3 [sonnet]: app/settings/connectors page + connect buttons (Composio connect flow), status reflect, disconnect. Match the square seafoam design system (tokens in app/get-started-stepper.tsx; seafoam #C1F5DF / ink #14140F / paper #FAFAF8; radius 0; Bricolage Grotesque; copy primaryButtonClass).
  D5 [sonnet]: audit/observability glue.
  Deliverable 11 [opus, after Calendar tool]: AR-9 calendar capture prompts (post-meeting + pre-meeting brief) on the AR-5 sweep cadence, calendar-metadata-only, counts toward Phase 3 caps.
Wave E — YOU (Fable): fake-transport E2E fixtures E1-E5 (one sonnet agent if substantial), full gates (pnpm typecheck && pnpm test && pnpm build), then live wave with Arav: copy Composio keys into Vercel, apply migrations via psql, deploy, verify connector functions registered by behavior, then live: @Slack command from Arav's email -> approval email -> approve -> Slack DM actually delivered; @Calendar command -> confirmation -> event lands on his calendar; revoke -> reconnect email. Remove the temp admin probe route. Flag the Postmark upgrade.

YOUR VERIFICATION DUTIES (non-negotiable):
- Read every subagent diff before accepting. Adversarial focus: the execute-once invariant (deliver the same approval event twice -> exactly one Composio call, one connector_actions row), the AR-7 gate (no action runs without a live approved approval; replay/stale/rejected approvals denied), the AR-9 provenance boundary (no prompt ever uses anything beyond calendar metadata + captured loops), and recipient-ambiguity blocking approval.
- Run pnpm typecheck && pnpm test between waves; full pnpm build before deploy. New code needs new tests with fake transports — no live Slack/Google/Composio in the default test run.
- Commit per task, atomic, message style "Phase 4 <task>: ..." ending with "Co-Authored-By: Claude <noreply@anthropic.com>". Never commit secrets; never print secret values.
- If a subagent's work fails review twice, fix it yourself rather than spawning a third attempt.

Working style: act, don't ask, for reversible engineering steps. Arav does: anything on his phone, Composio/Clerk/Postmark dashboard reads you can't reach via CLI, copying Composio keys into Vercel, authorizing Slack/Calendar in Composio, sending real emails, and the Postmark upgrade. Stop at the first live-smoke failure, debug yourself (psql, vercel logs, Composio status, probes) before involving him. Start with Wave 0 (Composio research) in parallel with Wave A's A1/A4.
```
