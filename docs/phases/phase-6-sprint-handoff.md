# Phase 6 Sprint Handoff (Reliability, Evaluation & Trust Hardening)

Orchestrated sprint: an Opus orchestrator oversees and verifies; subagents (Sonnet/Opus per task) write the code in isolated git worktrees. Launch a fresh session **on Opus**:

```sh
cd /Users/aravb/Developer/keeps && claude
# then: /model → Opus, and paste the Prompt below
```

Phase 6 is **large** (15 deliverables). It does not all have to land in one session — checkpoint after each wave, keep `main` green, and it's fine to run it across several sessions. If you want to scope to the pilot-blocking subset first, do: **eval suite + delete/export/retention + Sentry + dead-letter + Postmark deliverability**, and defer the metrics dashboards/admin polish.

---

## Prompt

```
You are the Opus orchestrator for Phase 6 (Reliability, Evaluation & Trust Hardening) of Keeps. Working directory: /Users/aravb/Developer/keeps. You orchestrate and verify — you do NOT write feature code yourself. Spawn a subagent per task with the Agent tool (isolation:"worktree", model per the assignments below), review every diff critically before accepting, run the gates between waves, cherry-pick reviewed commits onto main, and run the live/ops steps with Arav. Ground truth: docs/phases/phase-6-reliability-eval-trust.md (the executable plan — 15 deliverables, waves A–E with exact files/acceptance), AR-1..AR-9 in docs/phases/README.md, and git log. Read docs/phases/phase-5-sprint-handoff.md too — it shows the exact orchestration mechanics that worked for Phase 5 (worktree subagents → review → cherry-pick → gates → live wave).

THE GOAL (one sentence): make Keeps dependable enough to invite the first 5–10 pilot users onto real work email without babysitting — measurable extraction quality (versioned fixture eval suite + precision/recall gate), single-surface observability (Sentry across app + Inngest + webhooks), full trust controls (see/export/delete data, per-email delete, raw-email retention scrub that keeps derived loops + quotes), instrumented model-call logs with redaction, Postmark bounce/complaint suppression, and reliability (explicit per-function retries + a dead-letter/replay queue + verified idempotency at every layer).

CURRENT STATE (verified 2026-06-13 — do not re-derive):
- Phases 0–5 are ALL DONE and LIVE at https://keeps.email (Vercel project `keeps`, aliased). The full single-user loop runs in prod: capture → extract loops → nudge/digest → approve → act (Slack/Calendar via Composio) → ask "what are my insights?" → ranked reply + signed /r/<token> memo page with row actions. ~1072 tests green (DB-gated ones skip without TEST_DATABASE_URL).
- Phase 6's waves A–E in the plan are gated on "Phase 3/4/5 done" — those gates are ALL satisfied now, so the only real ordering is Phase-6-INTERNAL dependencies (re-cut below). Parallelize aggressively.

CRITICAL CORRECTIONS TO THE PLAN (it was written before Phases 4–5; follow THESE):
1. DB is AWS RDS, NOT Neon (the plan's precondition says Neon — wrong). `pnpm db:migrate` / `pnpm db:generate` are BROKEN BY DESIGN (no drizzle journal). ALL migrations are HAND-WRITTEN sequential idempotent SQL in src/db/migrations/. Next number is 0015 (0013/0014 are Phase 5). Copy 0008 style for new TABLES + new ENUM TYPES (CREATE TYPE in a DO $$ ... duplicate_object guard + CREATE TABLE/INDEX IF NOT EXISTS). Copy 0009/0014 style for ENUM-VALUE additions (ALTER TYPE ... ADD VALUE IF NOT EXISTS) and put those in their OWN separate non-transactional file (ALTER TYPE ADD VALUE cannot run in a transaction). So: new tables/columns/enum-types → one file; nudge_status 'failed' + the audit_action additions → a SEPARATE file. Apply to local dev Postgres (postgres://postgres:postgres@localhost:55433/keeps via /opt/homebrew/opt/libpq/bin/psql) TWICE to prove idempotency; the orchestrator applies to prod in the ops wave (DATABASE_URL via `doppler secrets get DATABASE_URL --project keeps --config prd --plain`). Drizzle schema additions in src/db/schema.ts mirror existing table style; export the new types.
2. Phase 4 connectors are COMPOSIO, not Nango. connector_actions table EXISTS with a unique idempotency_key (src/db/schema.ts) and connector.action_failed is already emitted by the connector command handler. Deliverable 12/15/D1/D2 build on the REAL files: src/connectors/* and src/workflows/functions/handle-connector-command.ts. Do NOT touch the connector execution path beyond adding the failure-alert subscriber + a double-execute regression test.
3. Phase 5 is DONE: generated_reports table exists; deletion (del 7/8) + export (del 9) MUST cover generated_reports. The report summarizer (src/reports/summarize.ts defaultGenerateSummary) is a model call that must route through the instrumented wrapper (del 5).
4. THE MODEL CALLERS to route through src/agent/instrumented-generate-object.ts (del 5 / C4): grep for `generateObject` across src/ — current callers are src/agent/extract-loops.ts (extract_loops), src/agent/classify-intent.ts defaultGenerateEntity (classify_intent), src/agent/parse-connector-command.ts (draft_slack/draft_calendar), src/reports/summarize.ts (summarize_report), and any Phase 3 nudge drafter. Route ALL of them; the wrapper must PRESERVE the OpenAI strict-output contract (don't reshape schemas).
5. NO GIT REMOTE. The plan's A4 `.github/workflows/eval.yml` cannot actually run (the repo isn't on GitHub; deploys are `vercel deploy --prod --yes` as user abharw). Create the workflow file for the future, BUT ALSO wire `pnpm eval` as a LOCAL gate the orchestrator runs before each deploy (and document that the "CI gate" is currently the pre-deploy gate until a remote exists). Don't claim CI enforcement that isn't wired.
6. Admin pages (/admin/*) need an `isAdmin boolean` on users (the plan assumes it). Add the column in the foundation migration; Arav flags his own user (`UPDATE users SET is_admin=true WHERE email='arav@basicsoftware.ai'`) — that's an Arav step.
7. Settings already exist at app/settings/page.tsx (digest prefs) + app/settings/connectors/page.tsx, with app/settings/_ui.ts tokens and a layout. The A6 "settings split" ADDS /settings/privacy, /settings/data, /settings/audit + tab nav WITHOUT breaking the existing pages. Auth pattern: auth() → user_identities(provider='clerk') → users.id (see app/settings/page.tsx).
8. Export file storage: there is NO S3 provisioned. Use Vercel Blob (private) for the export file + a signed expiring URL, OR email the JSON inline for small exports. Decide with Arav; default to Vercel Blob private. Clerk Backend SDK (for del-7 user deletion) uses the existing Clerk secret key in env.

ENGINEERING GOTCHAS — put the relevant ones in every subagent prompt VERBATIM:
1. OpenAI strict structured outputs: every generateObject Zod schema has NO .default()/.optional() — all fields required, optionality via .nullable(); no nested discriminatedUnion/oneOf. (Caused a prod outage, commit 77717a3.) Every model path keeps a deterministic no-model fallback so tests need NO creds (AR-8). CI/default test run has no OPENAI_API_KEY.
2. Inngest determinism: the function body re-executes per step. Anything random/time-based (now, ids, tokens) MUST be minted INSIDE a step.run and read from its memoized return; pass only primitives across step boundaries (Date → ISO, rehydrate). Functions register in app/api/inngest/route.ts and auto-sync on deploy via the Vercel↔Inngest integration (verify by behavior). Idempotency is a CEL expression on event.data (cannot hash inline) — precompute any composite key on the emitter.
3. Tests run in the NODE vitest env (vitest.config.ts environment:"node") — there is NO jsdom/testing-library. For any React render test use renderToStaticMarkup from react-dom/server (mock next/navigation's useRouter if a client component is in the tree). DB-gated tests run with TEST_DATABASE_URL=postgres://postgres:postgres@localhost:55433/keeps and use the DB-injectable repository constructor pattern (see DrizzleConnectorAccountsRepository / DrizzleReportsRepository: constructor(db?) defaulting to getDb()).
4. Worktree mechanics: each subagent symlinks node_modules first (`ln -s /Users/aravb/Developer/keeps/node_modules node_modules`), then `pnpm exec tsc --noEmit` + `pnpm exec vitest run <its files>`. The orchestrator: review every diff, cherry-pick the SHA onto main, then `git worktree remove --force` + `git branch -D` EVERY worktree BEFORE running gates (vitest scans .claude/worktrees and double-counts; if a stale/locked worktree exists, `git worktree prune`). Run the real gate scoped to the project tree: `pnpm exec vitest run src app` (and `pnpm test` once worktrees are clean).
5. Deletion is IRREVERSIBLE and trust-critical — del 7/8 cascade behavior must be tested against REAL local Postgres (55433), asserting the OTHER user is untouched, orphaned nudges are hard-deleted, audit rows handled per del-7 step 3, and idempotent replay. Retention scrub (del 10) must PRESERVE source_evidence.quote + loops while nulling raw bodies; test with an injected clock at the retention boundary + idempotent second pass.
6. Trust copy / outward-facing surfaces (privacy page, onboarding promise, deletion UX, any user-facing email) — DRAFT them but confirm wording with Arav before shipping (E4).

ORCHESTRATION PLAN — re-cut around Phase-6-INTERNAL dependencies (everything the plan gated on Phase 3/4/5 is unblocked). Map to parallel worktree subagents; commit per task; review every diff; cherry-pick onto main; remove worktrees before gates.

WAVE 0 — foundation migration (do FIRST, alone, so later tasks share the schema):
  W0 [sonnet]: ONE migration pair (0015 tables/columns/enum-types + 0016 enum-value additions) covering ALL Phase 6 schema up front so parallel agents don't fight migrations: tables eval_runs, eval_cases, model_calls, quality_metrics_daily, data_deletion_requests, failed_processing; columns users.outbound_email_state(+enum), users.raw_email_retention_days, users.is_admin, inbound_emails.scrubbed_at, email_messages.scrubbed_at; enum-value adds nudge_status 'failed' + the 8 audit_action values (del Data&Migrations). Mirror schema.ts + export types. Apply to local 55433 twice. This is the only schema task; all other agents import the resulting types.

WAVE A — measurement + observability + idempotency (all parallel after W0; independent files):
  A1 [opus]: eval harness — src/agent/eval/{types,load-cases,matcher,cli}.ts + matcher.test.ts + cli.test.ts + baseline.json + README.md (PII scrub procedure) + `eval` script in package.json + .github/workflows/eval.yml (future) AND wire `pnpm eval` as the pre-deploy gate. eval_runs row per invocation. (Deliverables 1,2,3-extraction-metrics.) Self-test per plan Testing.
  A2 [sonnet]: 10 synthetic eval cases in src/agent/eval/cases/synthetic-*.case.ts (every loopKind/basis/due-date variant + empty body + low-confidence + command/approval/correction/question). The 15 anonymized REAL cases are HUMAN-GATED (need Arav to supply/scrub source material — scaffold the format + a `real-*.case.ts` example + the scrub doc; flag the rest for Arav).
  A3 [sonnet]: Sentry — instrumentation.ts + instrumentation-client.ts + src/observability/{sentry.ts(beforeSend PII scrub),inngest-sentry.ts(withInngestSentry)} + env additions; wrap process-email/send-nudge/connector handlers + tag webhook routes. (Deliverable 4.) Arav must create the Sentry project + add SENTRY_DSN to Vercel env (ops step).
  A4 [opus]: model-call logging — src/agent/instrumented-generate-object.ts (records to model_calls, promptPreview null by default) + route EVERY generateObject caller through it (grep first; see correction #4) + /admin/model-calls page (isAdmin gated). (Deliverable 5.) Preserve strict-output schemas exactly.
  A5 [sonnet]: idempotency audit — docs/observability/idempotency.md + regression tests (double-POST inbound one-row/one-event in src/email/inbound.test.ts; state workflow/nudge/connector layers). (Deliverable 12.)
  A6 [sonnet]: settings split — app/settings/{privacy,data,audit}/page.tsx + app/settings/layout.tsx tab nav, WITHOUT breaking existing settings pages; wire the retention select into /settings/privacy; deletion/export/audit pages stub-render (handlers land in Wave B). (Deliverable: A6 + del-10 UI.)
  A7 [sonnet]: retention purge — src/workflows/functions/raw-email-retention-purge.ts(+test, injected clock) using the W0 columns; register in inngest route; scrub raw bodies but PRESERVE source_evidence.quote + loops; idempotent via scrubbed_at. (Deliverable 10.)
  A8 [sonnet]: Postmark deliverability — app/api/postmark/webhook/route.ts + src/email/deliverability.ts(+test) (Bounce/SpamComplaint/Delivery, shared-secret verify) + outbound sender suppression check consulting users.outbound_email_state (skip + nudges.status='skipped' reason). (Deliverables 6 + C2.) Arav configures the Postmark webhook URL (ops step).

WAVE B — trust controls + metrics (after W0 + A6; B1–B4 wire into A6 pages; parallel):
  B1 [sonnet]: per-email deletion — app/api/data/delete-email/route.ts + src/data/delete-email.ts(+DB-gated test). Cascade + orphan-nudge hard-delete + audit row. (Deliverable 8.)
  B2 [opus]: account-wide deletion — app/api/data/delete/route.ts + src/workflows/functions/process-data-deletion.ts(+DB-gated test) + Clerk Backend SDK delete + pending_inbound_emails purge by sender + explicit audit purge + final hashed user.deleted row + idempotent on dataDeletionRequestId. (Deliverable 7.) THE riskiest task — review cascade + idempotency adversarially.
  B3 [sonnet]: JSON export — app/api/data/export/route.ts + src/workflows/functions/{generate-data-export,send-export-email}.ts + docs/data-export-schema.md; include generated_reports + connector_actions (NO tokens); Vercel Blob private + signed 24h link (or inline if small). (Deliverable 9.)
  B4 [sonnet]: audit view — app/settings/audit/page.tsx (fill A6 stub) + src/audit/list-for-user.ts; "Download all" reuses export. (Deliverable 11.)
  B5 [sonnet]: metrics crons — src/workflows/functions/{score-nudge-feedback,score-draft-feedback}.ts writing quality_metrics_daily (false_positive_nudge_rate, draft_approval_edit_rate); mirror latest eval precision/recall into quality_metrics_daily. (Deliverable 3 production side.)

WAVE C — reliability hardening + connector alerting (after W0; some touch process-email/send-nudge so serialize those edits):
  C1 [opus]: dead-letter — src/workflows/dead-letter.ts shared helper + failed_processing writes from process-email final-failure + email.processing_failed event + /admin/failed-processing page + replay route + scripts/replay-failed-processing.ts CLI. (Deliverables 13 partial + 14.)
  C2 [sonnet]: retry-policy audit — set explicit `retries` on every Inngest function per del 13; send-nudge final-failure → nudges.status='failed' + nudge.failed event + outbound-nudge idempotency (X-Keeps-Idempotency-Key + re-read status in step). (Deliverables 12 nudge + 13 + B5-adjacent.) Coordinate file ownership with C1 over process-email/send-nudge (do C1 first, then C2, or one agent owns both files).
  C3 [sonnet]: connector failure alerting — src/workflows/functions/notify-connector-failure.ts (Sentry breadcrumb + quality_metrics_daily.connector_failures_24h + reconnect email after 3/hr via suppressed-aware sender) + double-execute connector idempotency regression test. (Deliverables 15 + D2.)

WAVE D — YOU (orchestrator) + one agent: dashboards, copy, full gates, ops, live verification:
  D1 [sonnet]: /admin/quality dashboard (quality_metrics_daily series + latest eval_runs) + /admin/deliverability page (suppressed users + reactivate). (Deliverables E3 + 6 admin.)
  D2 [sonnet]: privacy copy — /privacy marketing page + onboarding privacy promise referencing retention default + delete/export. (E4 — Arav confirms wording.)
  Then YOU: full gates (pnpm typecheck && pnpm exec vitest run src app && pnpm build); apply 0015/0016 to prod via psql; have Arav create the Sentry project + add SENTRY_DSN, configure the Postmark bounce/complaint webhook URL, set is_admin=true on his user, and supply/scrub the 15 real eval cases; vercel deploy --prod --yes; verify new Inngest functions registered by behavior; LIVE wave with Arav — trigger a bounce (suppress), run `pnpm eval` and confirm the gate, delete a test email (cascade), request an export (receive the link), confirm a retention scrub on an aged fixture, force a process-email failure and replay it from /admin/failed-processing. Update docs/phases/README.md Phase 6 row → done; write the Closeout; update the keeps-status memory.

YOUR VERIFICATION DUTIES (non-negotiable):
- Read every subagent diff before accepting. Adversarial focus: (a) DELETION correctness — cascades hit exactly the target, other users untouched, orphans cleaned, idempotent replays; (b) RETENTION — raw scrubbed but loops + source_evidence.quote survive, idempotent, "until I delete" never scrubs; (c) MODEL BOUNDARY preserved through the instrumented wrapper (strict schemas intact, deterministic fallbacks intact, prompt never persisted beyond opt-in 200-char preview); (d) IDEMPOTENCY at all four layers proven by tests; (e) SENTRY/PII — no raw bodies in error payloads; (f) SUPPRESSION — non-active users never receive outbound.
- Run pnpm typecheck && (scoped) pnpm test between waves; full pnpm build before deploy. New code needs new tests with deterministic fallbacks — no live model/Sentry creds in the default test run.
- Commit per task, atomic, message style "Phase 6 <task>: ..." ending "Co-Authored-By: Claude <noreply@anthropic.com>". Never commit secrets.
- If a subagent's work fails review twice, fix it yourself rather than spawning a third attempt.

OPS REALITIES (from Phases 4–5, still true): no git remote — deploy with `vercel deploy --prod --yes` (CLI authed as abharw, project keeps). Prod DATABASE_URL via doppler (keeps/prd); apply prod migrations by hand via psql at /opt/homebrew/opt/libpq/bin. Vercel env vars are SENSITIVE/write-only (Doppler/Vercel are the ops path). Inngest auto-syncs on deploy (verify by behavior; introspection GET is auth-protected). Postmark is on the free 100/mo tier — bounce/complaint webhooks + export emails add volume; the $15/mo upgrade is deferred until volume-limited (Arav's call).

LIVE-WAVE EMAIL AUTOMATION: prefer the COMPOSIO HOSTED GMAIL MCP (we already use Composio for Slack/Calendar; COMPOSIO_API_KEY is in Doppler backend/prd; the gmail toolkit is available on the account). If a gmail MCP is connected (check ToolSearch for GMAIL_SEND_EMAIL / GMAIL_FETCH_EMAILS), USE IT to automate the round-trip — send the test command from arav@basicsoftware.ai to the Keeps capture address, read the reply, extract the /r/<token> link — leaving only genuinely-human steps (a phone tap on a row, confirming a real deletion). To set it up: create a Gmail auth config (`ac_…`) + a hosted MCP server scoped to the gmail toolkit (allowed_tools GMAIL_SEND_EMAIL+GMAIL_FETCH_EMAILS) via the Composio API/dashboard, have Arav OAuth-connect his arav@basicsoftware.ai mailbox, then `claude mcp add --transport http gmail-composio "<MCP_URL>" --headers "X-API-Key:<COMPOSIO_API_KEY>"` and restart. If NO gmail MCP is available, exercise the pipeline programmatically: POST a Postmark-shaped payload to /api/email/inbound (shared-secret header) or send the Inngest event directly — covers everything except real Postmark send/receive + DKIM (one real round-trip with Arav).

Working style: act, don't ask, for reversible engineering steps. Arav does: prod deploy/migration go-aheads (irreversible), creating the Sentry project + adding SENTRY_DSN, configuring the Postmark webhook, flagging his own admin, supplying/scrubbing the 15 real eval cases, and any live UAT that sends real email or deletes real data. Stop at the first live-smoke failure and debug yourself (psql, vercel logs, Sentry) before involving him. Phase 6 is large — checkpoint after each wave, keep main green, and it's fine to run across multiple sessions. Start with Wave 0 (the foundation migration), then fan out Wave A.
```
