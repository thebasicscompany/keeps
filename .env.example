# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Phase 0 auth is a local dev stub. Replace with Clerk/Better Auth/WorkOS in a later phase.
KEEPS_DEV_AUTH_SECRET=replace-with-a-long-random-string

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/keeps

# Email provider
POSTMARK_SERVER_TOKEN=
KEEPS_INBOUND_WEBHOOK_SECRET=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.1

# Inngest
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
